{% extends 'jenkins-job.default.xml.twig' %}

{% block setup %}{% endblock setup %}

{% block command %}<![CDATA[
    docker run -i --rm \
        ${SPRYKER_DOCKER_RUN_ARGUMENTS} \
        --env-file=/var/spryker/env/cli/{{ job.store|lower }}.env \
        ${SPRYKER_DOCKER_RUN_IMAGE} \
        bash -c \
        "{{ job.command }}"
]]>{% endblock command %}
