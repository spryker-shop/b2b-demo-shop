{% extends template('page-layout-company', 'CompanyPage') %}

{% define data = {
    idCompanyUserInvitation: _view.idCompanyUserInvitation,
    title: 'company.user.invitation.title.plural' | trans,
    activePage: 'users'
} %}

{% block companyContent %}
    {% include molecule('action-bar') with {
        data: {
            title: 'company.user.invitation.delete.title' | trans
        }
    } only %}
    {{ "company.user.invitation.delete.confirmation.question" | trans }} <br><br>

    <div>
        <a href="{{url('company/user-invitation')}}" class="button button--info">{{ "general.back.button" | trans }}</a>
        <a href="{{url('company/user-invitation/delete/confirm', {'idCompanyUserInvitation': data.idCompanyUserInvitation})}}" class="button button--alert">{{ "company.user.invitation.delete.button" | trans }}</a>
    </div>
{% endblock %}
