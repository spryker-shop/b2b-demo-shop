{% extends model('component') %}

{% define config = {
    name: 'packaging-unit-quantity-selector',
    tag: 'packaging-unit-quantity-selector',
} %}

{% define attributes = {
    json: required,
} %}
