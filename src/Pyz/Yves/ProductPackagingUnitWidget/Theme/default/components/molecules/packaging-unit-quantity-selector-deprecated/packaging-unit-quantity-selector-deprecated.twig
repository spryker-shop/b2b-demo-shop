{% extends model('component') %}

{% define config = {
    name: 'packaging-unit-quantity-selector-deprecated',
    tag: 'packaging-unit-quantity-selector-deprecated'
} %}

{% define data = {
    json: required
} %}

{% block body %}
    {% include atom('json-scheme', 'ProductPackagingUnitWidget') with {
        class: config.name,
        data: {
            name: config.name,
            json: data.json
        }
    } only %}
{% endblock %}
