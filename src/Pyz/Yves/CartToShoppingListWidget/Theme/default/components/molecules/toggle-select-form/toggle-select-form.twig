{% extends model('component') %}

{% define config = {
    name: 'toggle-select-form',
    tag: 'toggle-select-form'
} %}

{% define data = {
    attribute: required,
    selectAttributes: {
        'data-select-trigger': '',
        'target': required,
        'class-to-toggle': required
    }
} %}


{%- block body -%}

    {{ form_row(data.attribute,  {'attr': data.selectAttributes}) }}

{%- endblock -%}

