{% extends model('component') %}

{% define config = {
    name: 'quote-request-cart-from',
    tag: 'div',
} %}

{% define data = {
    quoteRequestReference: required,
    cartForm: required,
} %}

{% block body %}
    {% if data.quoteRequestReference %}

        {{ form_start(data.cartForm) }}

        <div class="grid">
            <div class="col col--sm-12 spacing-bottom">
                <a class="button button--hollow button--expand" href="{{ path('agent/quote-request/cart/clear') }}" title="{{ 'quote_request_page.quote_request.actions.cancel' | trans }}">
                    {{ 'quote_request_page.quote_request.actions.cancel' | trans }}
                </a>
            </div>
            <div class="col col--sm-12 spacing-bottom">
                <button type="submit" name="save" class="button button--success button--expand">
                    {{ 'quote_request_page.quote_request.actions.save' | trans }}
                </button>
            </div>
            <div class="col col--sm-12 spacing-bottom">
                <button type="submit" name="saveAndBack" class="button button--success button--expand">
                    {{ 'quote_request_page.quote_request.actions.save_and_back_to_edit' | trans }}
                </button>
            </div>
        </div>

        {{ form_end(data.cartForm) }}

    {% endif %}
{% endblock %}
