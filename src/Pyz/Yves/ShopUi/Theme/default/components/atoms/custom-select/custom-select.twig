{% extends atom('select') %}

{% define config = {
    name: 'custom-select',
    tag: 'custom-select'
} %}

{% define attributes = {
    'selectWidth': '100%'
} %}

{% block selectClass %}{{ config.jsName }}{% endblock %}

