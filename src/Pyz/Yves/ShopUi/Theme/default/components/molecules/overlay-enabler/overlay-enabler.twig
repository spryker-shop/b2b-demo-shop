{% extends model('component') %}

{% define config = {
    name: 'overlay-enabler',
    tag: 'overlay-enabler'
} %}

{% define attributes = {
    'trigger-class-name': required,
    'toggle-overlay-modifiers': required,
} %}
