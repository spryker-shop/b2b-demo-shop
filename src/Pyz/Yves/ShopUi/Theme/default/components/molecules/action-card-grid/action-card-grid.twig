{% extends model('component') %}

{% define config = {
    name: 'action-card-grid',
    tag: 'div'
} %}

{% block body %}
    {% for card in data.cards %}
        <div class="{{ config.name }}__card">
            {{ card.content}}
        </div>
    {% endfor %}
{% endblock %}
