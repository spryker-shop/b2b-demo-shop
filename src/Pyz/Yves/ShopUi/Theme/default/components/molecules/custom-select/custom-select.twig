{% extends atom('select') %}

{% define config = {
    name: 'custom-select',
    tag: 'custom-select'
} %}

{% define attributes = {
    'config-width': 'resolve',
    'config-theme': 'default'
} %}

{% block selectClass %}{{ config.jsName }}{% endblock %}

