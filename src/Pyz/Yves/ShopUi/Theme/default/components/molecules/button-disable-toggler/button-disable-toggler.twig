{% extends model('component') %}

{% define config = {
    name: 'button-disable-toggler',
    tag: 'button-disable-toggler',
} %}

{% define attributes = {
    'trigger-form-class-name': required,
    'max-symbols': '1',
    'active-trigger-class-name': '',
} %}
