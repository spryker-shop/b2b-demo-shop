{% extends model('component') %}

{% define config = {
    name: 'slick-carousel',
    tag: 'slick-carousel'
} %}

{% define data = {
    slides: [],
    containerClass: ''
} %}

{% define attributes = {
    'slider-config': '{}'
} %}

{% block body %}
    <div class="{{config.name}}__container {{ data.containerClass }} js-{{ config.name }} is-hidden">
        {% block slides %}
            {% for slide in data.slides %}
                {{ slide }}
            {% endfor %}
        {% endblock %}
    </div>

{% endblock %}

