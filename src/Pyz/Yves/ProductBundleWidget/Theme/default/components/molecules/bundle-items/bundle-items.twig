{% extends molecule('bundle-items', '@SprykerShop:ProductBundleWidget') %}

{% block itemList %}
    {% set itemClassName = config.name ~ '__item' %}
    {% set thumbnailClassName = '' %}
    {% set thumbnailModifiers = ['with-overlay', 'pdp-bundle'] %}
    {% set itemDetailsClassName = config.name ~ '__details' %}

    {{ parent() }}
{% endblock %}
